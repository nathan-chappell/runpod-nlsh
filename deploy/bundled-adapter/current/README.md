# Bundled Adapter Staging

This directory is populated by `scripts/stage_serving_adapter.py` before building the serving image.

Tracked files here are placeholders only. The actual adapter weights and copied metadata stay git-ignored but remain visible to Docker builds.
